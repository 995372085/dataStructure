链表：
1) 链表是以节点的方式来存储的，是链式存储。
2）每个节点有两个域，一个data域（存储数据），一个next域（存储下一个节点）。
3) 链表分为带头节点( 单链表 )和不带头结点的链表
4) 头结点只存储下一个节点，不存储数据。

1.单链表    具有头结点，遍历需要辅助节点进行遍历
1) 增加节点
2) 有序增加节点
3) 修改节点   找到需要删除节点的上一个节点
4) 删除节点
5) 遍历节点
2.双链表
增加节点   修改最后一个节点的next和添加节点的pre
删除节点   直接找到需要删除的节点
          1）修改删除节点的pre节点的next为删除节点的next
          //删除节点不为最后一点才有第二步
          2）删除节点的next节点的pre为删除节点的pre节点
          因为如果删除的节点为最后一个节点，因为最后一个节点的next为null，所以2)需要进行判断是否为最后一个节点